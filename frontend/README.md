# 图宝前端

基于 Vue 3 + TypeScript + Vite 的现代化前端应用。

## 功能特性

- ✅ Vue 3 Composition API
- ✅ TypeScript 类型支持
- ✅ Pinia 状态管理
- ✅ Vue Router 路由管理
- ✅ Axios HTTP 客户端
- ✅ SSE 实时进度显示
- 🚧 生成器界面（待开发）
- 🚧 历史管理界面（待开发）

## 项目结构

```
frontend/
├── index.html           # 入口 HTML
├── package.json         # 项目配置
├── vite.config.ts      # Vite 配置
├── tsconfig.json       # TypeScript 配置
├── src/
│   ├── main.ts         # 应用入口
│   ├── App.vue         # 根组件
│   ├── style.css       # 全局样式
│   ├── vite-env.d.ts   # 类型定义
│   ├── router/         # 路由配置
│   │   └── index.ts
│   ├── store/          # 状态管理
│   │   └── index.ts
│   ├── services/       # API 服务
│   │   └── api.ts
│   └── views/          # 页面组件
│       ├── HomeView.vue
│       ├── GeneratorView.vue
│       └── HistoryView.vue
└── public/             # 静态资源
```

## 快速开始

### 1. 安装依赖

```bash
cd frontend
npm install
```

### 2. 配置环境变量

创建 `.env` 文件（可选）：

```env
VITE_API_URL=http://localhost:5030/api
```

### 3. 运行开发服务器

```bash
npm run dev
```

应用将在 `http://localhost:5173` 启动。

### 4. 构建生产版本

```bash
npm run build
```

## 页面路由

- `/` - 首页（主题输入）
- `/generator` - 生成器页面
- `/history` - 历史记录页面

## 状态管理

使用 Pinia 管理全局状态：

- `currentOutline` - 当前生成的大纲
- `progress` - 生成进度
- `isGenerating` - 生成状态
- `referenceImage` - 参考图片 URL

## API 服务

所有 API 调用通过 `src/services/api.ts` 进行：

- `generateOutline()` - 生成大纲
- `generateImages()` - 生成图片
- `getProgress()` - 获取进度（SSE）
- `getHistory()` - 获取历史记录
- `uploadReference()` - 上传参考图片

## 开发状态

### ✅ 已完成
- [x] 项目基础架构
- [x] 路由配置
- [x] 状态管理
- [x] API 服务封装
- [x] 首页界面

### 🚧 待开发
- [ ] 生成器界面
- [ ] 实时进度显示
- [ ] 图片预览和编辑
- [ ] 历史记录管理
- [ ] 一键下载功能

## 技术栈

- **框架**: Vue 3.3.8
- **语言**: TypeScript 5.3.2
- **构建工具**: Vite 5.0.5
- **路由**: Vue Router 4.2.5
- **状态管理**: Pinia 2.1.7
- **HTTP 客户端**: Axios 1.6.2

## 样式规范

- 使用 CSS Scoped 样式
- 渐变色主题：`#667eea` → `#764ba2`
- 卡片圆角：12px
- 按钮圆角：8px

## 注意事项

1. TypeScript 错误是正常的，需要先运行 `npm install` 安装依赖
2. 开发时确保后端服务已启动在 `http://localhost:5030`
3. Vite 已配置代理，`/api` 请求会自动转发到后端

## 下一步开发计划

1. 完善生成器界面
2. 实现 SSE 实时进度显示
3. 开发图片预览组件
4. 实现历史管理功能
5. 添加响应式设计