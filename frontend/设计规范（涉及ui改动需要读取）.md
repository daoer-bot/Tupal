# 🌌 Tupal - Aurora Glass Design System

> "Create with the flow of light."

本文档定义了 Tupal 全新 UI 的设计语言、色彩规范与组件标准。所有后续开发请严格遵循此系统，以保持视觉一致性。

---

## 1. 核心理念 (Core Philosophy)

*   **Aurora (极光)**：使用流动的渐变光晕作为背景，象征灵感的不确定性与创造力。
*   **Glass (玻璃)**：UI 元素悬浮于背景之上，通过磨砂质感 (Frosted Glass) 透出背景色，强调通透感与层级。
*   **Space (留白)**：大面积留白与宽松的间距，减少视觉噪音，让用户聚焦于创作本身。

---

## 2. 色彩系统 (Color Palette)

### 品牌色 (Brand Colors)
| 名称 | 变量名 | 色值 | 用途 |
| :--- | :--- | :--- | :--- |
| **Indigo** | `--primary-color` | `#6366f1` | 主色调，用于按钮、高亮、图标 |
| **Neon Pink** | `--secondary-color` | `#ec4899` | 辅助色，用于渐变、强调点 |
| **Dream Violet** | `--accent-color` | `#8b5cf6` | 装饰色，用于混合光晕 |

### 中性色 (Neutrals)
| 名称 | 变量名 | 色值 | 用途 |
| :--- | :--- | :--- | :--- |
| **Dark Blue** | `--text-primary` | `#1e293b` | 主要标题、正文 |
| **Slate Gray** | `--text-secondary` | `#64748b` | 次要文本、图标 |
| **Light Gray** | `--text-tertiary` | `#94a3b8` | 占位符、禁用态 |

---

## 3. 玻璃拟态系统 (Glassmorphism)

这是本设计系统的灵魂。请直接使用 CSS 类，避免手动编写样式。

### 基础面板 `.glass-panel`
适用于侧边栏、卡片、模态框等容器。

```css
.glass-panel {
  background: rgba(255, 255, 255, 0.65);
  backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.4);
  box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.07);
  border-radius: 24px;
}
```

### 强玻璃 `.glass-panel-heavy`
适用于需要更高不透明度，以保证内容可读性的区域（如输入框容器）。

```css
.glass-panel-heavy {
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(20px);
}
```

---

## 4. 排版 (Typography)

*   **字体栈**：`Plus Jakarta Sans`, `Inter`, System UI
*   **Hero 标题**：`3.5rem` (56px) / 800 Weight / 渐变填充
*   **卡片标题**：`1.125rem` (18px) / 600 Weight
*   **正文**：`1rem` (16px) / 400 Weight / 1.6 Line Height

### 渐变文字 `.text-gradient`
用于 Logo 和 Hero 标题，增加视觉张力。

```css
.text-gradient {
  background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
  -webkit-background-clip: text;
  color: transparent;
}
```

---

## 5. 组件规范 (Component Specs)

### 按钮 (Buttons)
*   **Primary**: 线性渐变填充，强阴影。
*   **Glass**: 半透明白色背景，白色描边，Hover 时变为品牌色。

### 输入框 (Inputs)
使用 `.glass-input`。默认状态下呈现半透明，Focus 时光晕加重。

### 侧边栏 (Sidebar)
*   **状态**：悬浮 (Floating)
*   **宽度**：展开 280px / 收起 88px
*   **动画**：`0.4s cubic-bezier`

---

## 6. 动效 (Motion)

*   **Float**: 缓慢的上下浮动，用于 Badge 或装饰元素。
*   **Pulse**: 缓慢的呼吸效果，用于 Logo 背景。
*   **Aurora**: 20s 循环的背景光斑位移，永不停止的流动感。